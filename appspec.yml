version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "Python:3"
        LoadBalancerInfo:
          ContainerName: "Python"
          ContainerPort: 80

